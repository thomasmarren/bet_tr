<h1>LEADERBOARDS</h1>

<%= column_chart(@chart2,
{library: {hAxis: {title: "Username"}, vAxis: {title: "Number of Winning Bets"}}}) %>

<% @bets.each_with_index do |bet, index| %>
<h3><%= index + 1 %>.
<%= bet.user.username %> -
<%= bet.count %> Wins
<br>
<% end %>
